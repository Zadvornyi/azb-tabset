import { TestBed, async } from '@angular/core/testing';
import { AzbTabSetComponent } from './azb-tabset.component';

describe('AzbTabSetComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        AzbTabSetComponent
      ],
    }).compileComponents();
  }));
});
